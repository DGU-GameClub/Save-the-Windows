using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tower16V3 : TowerUnit
{
    public void LifeUp() { 
        /*
         라운드가 끝나면 맵(하이어라키 창)에 있는 모든 v3 오브젝트를 가져와 이 함수를 실행. 
        GameManager.instance.Life = GameManager.instance.Life + Attack;
         */
    }
}
